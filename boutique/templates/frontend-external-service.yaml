apiVersion: v1
kind: Service
metadata:
  name: frontend-external
  labels:
    app: frontend
spec:
  type: ClusterIP # Instead of Loadbalancer
  selector:
    app: frontend
  ports:
  - name: http
    port: 80
    targetPort: 8080
